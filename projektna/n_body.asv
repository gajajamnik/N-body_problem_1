function dx = n_body(~, x) 

GM = 1e2; %gravitacijska konstanta
N = size(x, 1)/6; 

x = reshape(x, [6, N])';

dx(:, 1) = x(:,4);
dx(:,2) = x(:,5);
dx(:, 3) = x(:, 6);

for i = 1:size(x,1)
    dx(i, 4:6)=0;
    for j = 1:size(x,1)
        if j~=1
            df = x(j, 1:3)

dx ( : , 1 ) = 


end